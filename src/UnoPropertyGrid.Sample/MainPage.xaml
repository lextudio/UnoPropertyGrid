<Page
    x:Class="UnoPropertyGrid.Sample.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:pg="using:UnoPropertyGrid"
    RequestedTheme="Dark"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowSpacing="8" Padding="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ComboBox
                x:Name="ObjectSelector"
                MinWidth="260"
                Header="Selected object"
                SelectionChanged="ObjectSelector_SelectionChanged" />

            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                VerticalAlignment="Bottom"
                Spacing="8">
                <TextBlock
                    VerticalAlignment="Center"
                    Text="Theme" />
                <ToggleSwitch
                    x:Name="ThemeToggle"
                    OffContent="Light"
                    OnContent="Dark"
                    Toggled="ThemeToggle_Toggled"
                    IsOn="True" />
            </StackPanel>
        </Grid>

        <pg:PropertyGridControl
            x:Name="PropertyGrid"
            Grid.Row="1"
            NameColumnWidth="150"
            PropertyGridTheme="Dark" />
    </Grid>
</Page>
