<Page
    x:Class="UnoPropertyGrid.Sample.Aot.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:pg="using:UnoPropertyGrid"
    Background="#F3F3F3">

    <Grid RowSpacing="8" Padding="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Banner explaining the AOT mode -->
        <Border
            Grid.Row="0"
            Background="#EFF6FF"
            BorderBrush="#93C5FD"
            BorderThickness="1"
            CornerRadius="4"
            Padding="10,6">
            <TextBlock
                FontSize="12"
                Foreground="#1D4ED8"
                TextWrapping="Wrap">
                AOT mode — property descriptors are source-generated at compile time.
                No reflection or TypeDescriptor is used for property discovery.
            </TextBlock>
        </Border>

        <ComboBox
            x:Name="ObjectSelector"
            Grid.Row="1"
            MinWidth="260"
            Header="Selected object"
            SelectionChanged="ObjectSelector_SelectionChanged" />

        <pg:PropertyGridControl
            x:Name="PropertyGrid"
            Grid.Row="2"
            NameColumnWidth="160"
            PropertyGridTheme="Light"
            ShowDescriptionPane="True" />
    </Grid>
</Page>
